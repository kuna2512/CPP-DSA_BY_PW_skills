/*
Some inbuilt library functions :-
1. min(x,y)
2. max(x.y)
3. sqrt(x);
*/
#include<bits/stdc++.h>
using namespace std;
int main(){
    int x,y;
    cout<<"Enter the value of x : ";
    cin>>x;
    cout<<"Enter the value of y : ";
    cin>>y;
    cout<<"Min of x, y is : "<<min(x,y)<<endl;
    cout<<"Max of x, y is : "<<max(x,y)<<endl;
}