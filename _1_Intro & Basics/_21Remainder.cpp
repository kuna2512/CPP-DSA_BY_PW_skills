#include <iostream>
#include <cmath>
using namespace std;

int main(){
    float a, b;
    a = 76;
    b = 3;
    cout << fmod(a, b);
// it another way to print ramainder if we using float data type.
}
